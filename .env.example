# Banco de dados (Módulo 4).
# Pegue a connection string no Supabase:
#   Project Settings > Database > Connection string > "Transaction pooler".
# Copie este arquivo para .env.local e cole a sua URL abaixo.
# Sem esta variável, a Agenda roda com dados em memória.
DATABASE_URL=
